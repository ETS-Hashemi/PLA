% PLA — journal manuscript (Overleaf project root).
% Elsevier elsarticle class, as used by the target journals in
% research/GAP_STATEMENT.md (Knowledge-Based Systems, Expert Systems with
% Applications, Information Sciences).
%
% Compile: pdflatex main && bibtex main && pdflatex main && pdflatex main
% Class options: [review]  double-spaced with line numbers (submission)
%                [preprint] single-spaced preprint
%                [final,3p,times] journal-layout proof
% The 4-6 page workshop cut is the separate, self-contained workshop.tex.

\documentclass[review,12pt]{elsarticle}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{booktabs}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

\biboptions{numbers,sort&compress}

\newtheorem{proposition}{Proposition}
\newcommand{\pla}{\textsc{pla}}
\newcommand{\logit}{\operatorname{logit}}

\journal{Knowledge-Based Systems}

\begin{document}

\begin{frontmatter}

\title{\pla: A Transparent, Learnable Rule-Confidence Engine
with Measured Trace Fidelity}

\author{Seyed Masoud Hashemi Ahmadi}
\ead{contact@AiCentralLab.com}
\address{\'Ecole de technologie sup\'erieure (\'ETS), Montr\'eal, Canada}

\begin{abstract}
We present \pla{}, a rule-confidence model for domains where inherent
interpretability is a requirement rather than a preference. \pla{}
descends openly from MYCIN-style certainty factors: rules carry
confidences, conjunction is the G\"odel t-norm, and co-supporting rules
combine through a named, per-knowledge-base aggregation operator
(noisy-OR by default --- provably MYCIN's parallel-combination rule).
Three properties distinguish it from its lineage and from modern
statistical-relational systems: (i)~rule confidences are
\emph{context-conditioned}, with a log-odds mode in which context
variables contribute additive evidence weights that never saturate;
(ii)~both rule and context weights are \emph{learnable} --- on binary
facts the engine's log-odds path reduces exactly to logistic regression
over rule firings, making the loss convex and training provably
monotone; and (iii)~explanations are \emph{traces whose fidelity is
measured}, via deletion-based comprehensiveness and sufficiency against
a reversed-attribution control. The calculus has machine-checked
convergence guarantees, the entire engine is a few hundred lines of
dependency-free Python, and every experimental number regenerates from
committed scripts with bootstrap confidence intervals. On the real ULB
credit-card data an 11-rule model reaches AUC $0.96$--$0.97$ across
three split seeds --- within $0.02$ of the interpretable accuracy
frontier (EBM, $0.98$) --- with near-perfect calibration. On
accounting-fraud data under two temporal designs, including one built
around the 2003 SOX regime change, pre-registered ablations find no
reliable ranking benefit from context conditioning; we report that
negative as such. The demonstrated value of learning is calibration,
and the deliverable is the measured, counterfactual-bearing trace.
\end{abstract}

\begin{keyword}
probabilistic logic \sep certainty factors \sep context-aware reasoning
\sep interpretable machine learning \sep explainable AI \sep
explanation fidelity \sep fraud detection
\end{keyword}

\end{frontmatter}

\input{sections/01-introduction}
\input{sections/02-related}
\input{sections/03-calculus}
\input{sections/04-learning}
\input{sections/05-implementation}
\input{sections/06-evaluation}
\input{sections/06b-discussion}
\input{sections/07-limitations}
\input{sections/08-conclusion}

\bibliographystyle{elsarticle-num}
\bibliography{references}

\appendix
\input{sections/appendix}

\end{document}
