\section{Experimental evaluation}\label{sec:evaluation}

\subsection{Experimental setup}\label{sec:setup}

\paragraph{Datasets and splits.} Four designs, all regenerated by the
committed scripts. (i)~A seeded synthetic harness validation
($n{=}2{,}000$, planted logistic ground truth; 70/30 split).
(ii)~The ULB credit-card fraud data~\cite{dalpozzolo2015}
(284{,}807 transactions, 492 frauds, 0.17\%; verified against its
published invariants) under random 70/30 splits with seeds 42, 43, 44;
199{,}364 train / 85{,}443 test. (iii)~The official replication data of
Bao et al.~\cite{bao2020} (146{,}045 firm-years, 964 AAER frauds) under
a \emph{strict} temporal design (train through fiscal 2001, gap 2002,
test from 2003) and (iv)~a \emph{SOX-boundary} design (train through
2005 --- spanning the 2003 regime change, so a PostSOX context weight is
identifiable --- gap 2006--07, test from 2008). Bao training sets drop
post-first-fraud firm-years, approximating the original serial-fraud
protocol (602 and 1{,}172 firm-years dropped respectively; counts
recorded in the generated files); firm-years with unparseable ratios are
complete-case deleted for every model alike.

\paragraph{Propositionalization and rule mining.} Continuous features
become propositions via decile thresholds ($q{=}0.1$ and $q{=}0.9$,
fitted on the training split only): \texttt{V14\_low},
\texttt{Amount\_high}, and so on. Candidate rules are mined on the
training split by empirical precision with support floors: the top 8
single-proposition rules (support $\ge 20$) plus the top 3 pair rules
(support $\ge 10$, pairs drawn from the top 5 singles) --- 11 rules per
design, each carrying its empirical precision as the static confidence.
No per-dataset tuning of these mining constants was performed; they are
the pipeline defaults.

\paragraph{\pla{} variants.} \texttt{static} scores by noisy-OR over
the fired rules' empirical precisions (the engine's default semantics;
the ProbLog row cross-checks the fold). \texttt{learned} fits rule and
context weights plus the intercept through the logit path
(Section~\ref{sec:learning}; full-batch gradient descent, 400 epochs,
learning rate $1.0$). \texttt{learned no-context} is the pre-registered
ablation: identical training with context variables removed, isolating
the context mechanism.

\paragraph{Baselines.} Two equal-interpretability competitors --- a
depth-4 class-balanced decision tree and an Explainable Boosting
Machine at library defaults~\cite{lou2013,nori2019} --- plus logistic
regression (raw features, \texttt{max\_iter}=2000), gradient boosting
(scikit-learn histogram GBT) both untuned and class-weighted, ProbLog
executing \pla{}'s own rules under distribution semantics (per-example
programs; deterministic subset, $n{=}150$, for compilation cost), and a
pgmpy naive Bayes over the same propositions (deterministic subset,
$n{=}2000$). Subset rows are reported for cross-validation of the
semantics, not for ranking comparison.

\paragraph{Metrics.} ROC AUC and average precision (AP; the
class-imbalance-sensitive complement) both carry seeded
percentile-bootstrap 95\% confidence intervals ($B{=}500$, shared
resamples); log-loss and expected calibration error (10 equal-width
bins) measure calibration. All values are exactly as generated (four
decimals) by \texttt{scripts/run\_experiments.py}; prevalence sets the
AP baseline of a random ranker (e.g.\ $0.0017$ on credit card).

\subsection{Synthetic harness validation}

Table~\ref{tab:headtohead} validates the pipeline on data with planted
logistic ground truth. With the intercept included, learning matches the
static rules' ranking (AUC $0.7227$ vs.\ $0.7217$) while winning
calibration decisively (log-loss $0.3420$ vs.\ $1.0908$); both trail the
raw-feature models, as expected when the true signal is linear in the
continuous features and discretization discards most of it --- a tax
that average precision magnifies ($0.8470$ for logistic regression vs.\
$0.3139$ learned). The ProbLog
row executes the same rules under distribution semantics and its
interval contains \pla{} static's point estimate, cross-validating the
noisy-OR implementation.

\begin{table}[t]
\centering
\caption{Synthetic development sample (30\% test); generated by
\texttt{scripts/run\_experiments.py}. Subset rows (below the rule) are
cross-checks, not ranking comparisons.}
\label{tab:headtohead}
\begin{tabular}{lrlrrr}
\toprule
Model & AUC & AUC 95\% CI & AP & log-loss & ECE\\
\midrule
logistic regression (raw features) & 0.9746 & [0.9602, 0.9857] & 0.8470 & 0.1390 & 0.0298\\
EBM (raw features) & 0.9572 & [0.9376, 0.9729] & 0.7640 & 0.1917 & 0.0449\\
gradient boosting (class-weighted) & 0.9197 & [0.8935, 0.9437] & 0.6302 & 0.2900 & 0.0712\\
gradient boosting (untuned) & 0.9171 & [0.8880, 0.9430] & 0.6352 & 0.3750 & 0.0808\\
pgmpy naive Bayes (propositions) & 0.7228 & [0.6594, 0.7791] & 0.3288 & 0.3405 & 0.0288\\
\pla{} learned (rules + context + bias) & 0.7227 & [0.6568, 0.7834] & 0.3139 & 0.3420 & 0.0290\\
\pla{} static (empirical precisions) & 0.7217 & [0.6579, 0.7785] & 0.2947 & 1.0908 & 0.0952\\
decision tree (depth 4, balanced) & 0.7208 & [0.6668, 0.7710] & 0.2278 & 0.5492 & 0.1933\\
\pla{} learned, no context (ablation) & 0.7187 & [0.6529, 0.7792] & 0.3212 & 0.3422 & 0.0307\\
\midrule
ProbLog on \pla{} rules ($n{=}150$) & 0.7416 & [0.6351, 0.8413] & 0.4072 & 1.4447 & 0.0704\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Real data I: credit-card fraud}

Table~\ref{tab:creditcard} and Figure~\ref{fig:creditcard} report the
ULB dataset (70/30 split, seed 42, thresholds fitted on train only).

\begin{table}[t]
\centering
\caption{ULB credit-card fraud (real data; fraud rate 0.17\%; split seed
42). AP baseline of a random ranker: 0.0017. Subset rows (below the
rule) are semantics cross-checks on deterministic subsamples and are not
comparable to full-test rows.}
\label{tab:creditcard}
\begin{tabular}{lrlrrr}
\toprule
Model & AUC & AUC 95\% CI & AP & log-loss & ECE\\
\midrule
EBM (raw features) & 0.9815 & [0.9680, 0.9930] & 0.8642 & 0.0024 & 0.0002\\
logistic regression (raw features) & 0.9796 & [0.9647, 0.9915] & 0.7849 & 0.0036 & 0.0003\\
\pla{} static (empirical precisions) & 0.9687 & [0.9500, 0.9840] & 0.7258 & 0.0172 & 0.0138\\
\pla{} learned, no context (ablation) & 0.9630 & [0.9396, 0.9812] & 0.4951 & 0.0113 & 0.0022\\
\pla{} learned (rules + context + bias) & 0.9625 & [0.9381, 0.9817] & 0.5361 & 0.0113 & 0.0022\\
gradient boosting (class-weighted) & 0.9598 & [0.9323, 0.9857] & 0.7899 & 0.0179 & 0.0148\\
decision tree (depth 4, balanced) & 0.9074 & [0.8577, 0.9443] & 0.4947 & 0.1026 & 0.0696\\
gradient boosting (untuned) & 0.7159 & [0.6377, 0.7902] & 0.5316 & 0.0262 & 0.0009\\
\midrule
ProbLog on \pla{} rules ($n{=}150$) & 0.9610 & [0.8887, 0.9994] & 0.8960 & 0.1502 & 0.0553\\
pgmpy naive Bayes ($n{=}2000$) & 0.9322 & [0.7928, 1.0000] & 0.6190 & 0.0195 & 0.0016\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_creditcard_auc}
\caption{Credit-card fraud, split seed 42: AUC with bootstrap 95\% CIs
for all full-test models (generated by
\texttt{scripts/make\_figures.py}). \pla{} squares, baselines circles;
the 11-rule models sit between the raw-feature frontier and the
depth-4 tree.}
\label{fig:creditcard}
\end{figure}

Four findings. First, the accuracy frontier on this dataset is itself
interpretable: EBM tops the seed-42 table on both metrics (AUC
$0.9815$, AP $0.8642$), just above logistic regression, and one of the
two leads on every seed. Second, an 11-rule, fully hand-traceable
\pla{} model tracks that frontier at a measured distance: across the
three seeds \pla{} static spans AUC $0.9525$--$0.9687$ and learned
$0.9485$--$0.9628$, against $0.9700$--$0.9796$ for logistic regression
and $0.9747$--$0.9815$ for EBM --- gaps of $0.013$--$0.026$, with
overlapping confidence intervals on every seed; in average precision
the static rules retain most of the frontier's early ranking power
($0.6606$--$0.7258$ vs.\ $0.7535$--$0.7849$ for logistic regression).
Third, learning is a measured trade, not a free upgrade. Its gain is
calibration --- ECE $0.0022$, and Figure~\ref{fig:reliability} shows
the learned model hugging the diagonal at the low probabilities where
nearly all mass lives while the static rules' empirical precisions
overshoot there --- but average precision reveals a real early-ranking
cost that AUC conceals: learned AP is $0.5307$--$0.5522$ across seeds
against the static rules' $0.6606$--$0.7258$, a loss concentrated
exactly where an audit queue operates. Section~\ref{sec:discussion}
weighs this trade. Fourth, this study surfaced a model-class
pathology, preserved pre-fix in the repository's history: without the
intercept the learned variant's AUC was $0.076$ --- inverted --- for
the reason given in Section~\ref{sec:learning}; the intercept restores
it. A baseline note: untuned gradient boosting collapses at this class
imbalance (AUC $0.7159$), and simple class weighting repairs it (AUC
$0.9598$, AP $0.7899$) --- both rows are reported so neither defaults
nor a single tuning choice silently carries the comparison.

\begin{figure}[t]
\centering
\includegraphics[width=0.62\linewidth]{figures/fig_reliability_creditcard}
\caption{Reliability diagram on the credit-card test split (seed 42),
fixed log-spaced bins on log-log axes; points below $5\times10^{-5}$
are drawn at the display floor. The learned model hugs the diagonal at
the low probabilities where nearly all mass lives; the static rules'
empirical precisions overshoot there --- ranking survives, calibration
does not.}
\label{fig:reliability}
\end{figure}

\subsection{Real data II: accounting fraud under temporal drift}

Two temporal designs on the Bao et al.\ data (13 discretized ratios;
issuance indicator as context; the SOX design adds a PostSOX context
bit). In the strict design (62{,}087 training firm-years after
serial-fraud handling; 58{,}405 test), tail rules mined pre-2002 barely
rank later frauds: \pla{} static $0.5285$ [0.5028, 0.5585], learned $0.4905$,
no-context ablation $0.4733$, against $0.6794$ for raw-ratio logistic
regression and $0.6660$ for EBM. Average precision shows how thin the
absolute signal is even for the winners: logistic regression reaches
$0.0112$ against a test prevalence of $0.0061$ --- less than double the
random-ranker baseline. The context ablation shows a small positive AUC
gap ($+0.017$) whose intervals overlap. The SOX-boundary
design (82{,}630 training firm-years after serial-fraud handling;
32{,}530 test; Table~\ref{tab:sox}, Figure~\ref{fig:sox}) was built to
give the context weight its best chance.

\begin{table}[t]
\centering
\caption{Bao et al.\ accounting fraud, SOX-boundary design (train
$\le$2005, gap 2006--07, test $\ge$2008; 1{,}172 serial-fraud firm-years
dropped from training; fraud rate in test 0.40\%). Full-test models
only.}
\label{tab:sox}
\begin{tabular}{lrlrr}
\toprule
Model & AUC & AUC 95\% CI & AP & log-loss\\
\midrule
logistic regression (raw features) & 0.7003 & [0.6550, 0.7411] & 0.0080 & 0.0253\\
EBM (raw features) & 0.6441 & [0.6038, 0.6880] & 0.0067 & 0.0273\\
gradient boosting (class-weighted) & 0.6432 & [0.5969, 0.6849] & 0.0076 & 0.3448\\
gradient boosting (untuned) & 0.6283 & [0.5860, 0.6695] & 0.0062 & 0.0276\\
decision tree (depth 4, balanced) & 0.6266 & [0.5798, 0.6653] & 0.0061 & 0.5154\\
\pla{} static (empirical precisions) & 0.5381 & [0.4941, 0.5809] & 0.0085 & 0.0923\\
\pla{} learned (rules + context + bias) & 0.4651 & [0.4207, 0.5097] & 0.0038 & 0.0266\\
\pla{} learned, no context (ablation) & 0.4640 & [0.4234, 0.5099] & 0.0037 & 0.0265\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_sox_auc}
\caption{SOX-boundary design: AUC with bootstrap 95\% CIs. The visual
verdict on context conditioning is the complete overlap of the two
\pla{} learned rows; every rule model sits near chance while raw-ratio
models retain signal.}
\label{fig:sox}
\end{figure}

\paragraph{The pre-registered verdict on context conditioning.} Even
under the design built to make the context weight identifiable, the
mechanism adds nothing: learned $0.4651$ vs.\ ablation $0.4640$ AUC and
$0.0038$ vs.\ $0.0037$ AP, with essentially identical intervals and
calibration (Figure~\ref{fig:sox}). Combined with the AUC null on
credit card ($0.9625$ vs.\ $0.9630$) and the within-CI sliver on the
strict design, the conclusion is that context-conditioned rule weights,
\emph{with the coarse single-bit context variables tested here}, show
no reliable benefit --- reported plainly per the kill criterion
recorded in the repository's gap statement before these experiments
ran. One post-hoc observation cuts the other way and is reported as
such: on credit card the context variant holds a small, seed-stable
average-precision edge over its ablation ($0.5307$--$0.5522$ vs.\
$0.4930$--$0.4995$ across the three seeds) that does not recur under
either drift design. Average precision was not the pre-registered
metric, so this generates a hypothesis for the next design rather than
revising the verdict. The demonstrated value of learning lies in
calibration, not in context adjustment; Section~\ref{sec:discussion}
draws the drift lesson.

\subsection{Trace fidelity}

\begin{table}[t]
\centering
\caption{Trace comprehensiveness and sufficiency vs.\ the
reversed-attribution control, with the seeded paired-bootstrap 95\% CI
of the mean comprehensiveness difference (trace $-$ control); generated
by \texttt{scripts/run\_fidelity.py}. Comprehensiveness: higher is
better. Sufficiency: closer to zero is better.}
\label{tab:fidelity}
%%FILL-FIDELITY-TABLE
\end{table}

Deletion metrics in the faithfulness framing of~\cite{jacovi2020}:
comprehensiveness (prediction drop when the trace's top rule's facts are
removed) and sufficiency (prediction change when only those facts are
kept), against a control that ranks the same fired rules worst first.
The static model's traces beat the control on both metrics on all four
datasets, and the paired-bootstrap intervals on the comprehensiveness
difference exclude zero in every case
(Table~\ref{tab:fidelity}). %%FILL-FIDELITY-SENTENCE
For the calibrated learned model on rare-event data the
absolute-probability deltas compress to noise level --- correctly
ordered but uninformative at this scale; log-odds-unit variants are the
needed refinement (Section~\ref{sec:limitations}).

\subsection{Case study: one real fraud, traced}\label{sec:casestudy}

The generated exhibit (\texttt{results/case\_study\_creditcard.md})
walks the highest-scored true positive in the credit-card test split
through the engine. Eleven rules fire; the noisy-OR fold accumulates
line by line from the strongest pair rule
(V10\_low $\wedge$ V4\_high, empirical precision $0.1949$) to a static
score of $0.4929$ against a base rate of $0.0017$. The deletion
counterfactual quantifies the top factor: removing its two facts drops
the static score from $0.4929$ to $0.2552$ (learned: $0.0189$ to
$0.0069$). This artifact --- weighted domain rules whose provenance is
an auditable training-set measurement, folded by explicit arithmetic,
with a counterfactual attached --- is what none of the baselines
produce, and is the deliverable the audit setting requires.
